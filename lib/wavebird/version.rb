# frozen_string_literal: true

module Wavebird
  # Gem version. Sent upstream as part of the +User-Agent+/wrapper version so
  # wavebird can attribute traffic to this Rails port.
  VERSION = "0.1.0"
end

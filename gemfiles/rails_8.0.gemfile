# frozen_string_literal: true

# Rails 8.0 leg of the CI matrix. Sets the version the root Gemfile reads, then
# evaluates it, so dependencies live in exactly one place.
ENV["RAILS_VERSION"] = "8.0"

eval_gemfile File.expand_path("../Gemfile", __dir__)
